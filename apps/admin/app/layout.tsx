export const metadata = { title: 'Hisab Admin', description: 'Internal support console' };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
