org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# flutter_secure_storage requires compileSdk 37 (see app/build.gradle.kts).
# AGP 9.1.0's maximum *recommended* compileSdk is 36, so it warns on every build.
# The warning is advisory, not a compatibility statement — silence it rather than
# let a permanent warning train everyone to ignore build output.
android.suppressUnsupportedCompileSdk=37
